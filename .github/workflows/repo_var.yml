name: Repository secret

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: Afficher secrets et variables
      run: |
            echo "Variable : ${{ vars.MY_VAR }}"
            echo "Secret : ${{ secrets.DEMO_SECRET }}"




  
